  <div class="community-info">
    <%= render 'communities/info', community: @community, currentUser: @currentUser %>
  </div>

  <div class="community-main">
    <p class="page-title">ペナルティ新規作成</p>
    <div class="comment">
      <p>
        <b>ペナルティ作成のポイント</b><br>
        相手がゴール達成のモチベーションが下がるような内容は控えましょう。<br>
      </p>
    </div>
    <div>
      <%= form_with(model:[@community, @penalty], local:true) do |f| %>
        <p>内容</p>
        <div>
        <%= f.text_area :content, placeholder: "必須" %>
        </div>
        <p>実行によって回復するポイントト</p>
        <div>
        <%= f.text_field :point, placeholder: "必須" %>
        </div>
        <div>
        <%= f.submit '作成' %>
        </div>
      <% end %>
    </div>
  </div>