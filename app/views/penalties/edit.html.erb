  <div class="community-info">
    <%= render 'communities/info', community: @community, currentUser: @currentUser %>
  </div>

  <div class="community-main">
    <p class="page-title">ペナルティ編集</p>
    <div class="comment">
      <p>
        <b>制定ポイント</b><br>
        相手がゴール達成の意欲を失わないようなないようにしましょう。<br>
      </p>
    </div>
    <div>
      <%= form_with(model:[@community, @penalty], local:true) do |f| %>
        <div>
          <label><%= f.label :content, "内容" %><span class="form-note">必須</span></label>
          <div>
            <%= f.text_area :content %>
          </div>
        </div>
        <div>
          <label><%= f.label :point, "ペナルティ実行による回復ポイント" %><span class="form-note">必須</span></label>
          <div>
            <%= f.text_area :point, placeholder: "マイナス設定不可" %>
          </div>
        </div>
        <div>
          <%= f.submit "編集完了" %>
        </div>
      <% end %>
      <div>
        <span><%= link_to '削除', community_penalty_path(@community.id, @penalty.id), method: :delete %></span>
      </div>
    </div>
  </div>