<!DOCTYPE html>
<html>
  <head>
    <title>RuleBook</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= include_gon %> <!-- gem 'gon'利用に必要な記述 -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/a3d3283c4c.js" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
  </head>

  <body>
    <header>
      <div class="header-area">
        <div class="site-logo">
          <%= link_to top_path do %>
            <%= image_tag 'gachiru', size: '200x40' %>
          <% end %>
        </div>
        <div class="site-concept">
            感情に左右されない快適なパートナーシップ
        </div>
      </div>
      <div class="sign-in-user">
        <% if user_signed_in? %>
          <div class="sign-in-user-profile-image">
            <span>
              <% if @standbyRules.present? || @standbyPenalties.present? || @unacceptedUsers.present? %>
                <i class="fa-solid fa-circle notification-on-picture"></i>
              <% end %>
            </span>
            <% if current_user.profile_image.attached? %>
              <%= image_tag current_user.profile_image.variant(resize: "40x40"), class: "sign-in-user-profile-image-cta" %>
            <% else %>
              <%= image_tag("f_f_event_98_s512_f_event_98_0bg.jpg", style: "width: 40px; height: 40px;", class: "sign-in-user-profile-image-cta") %>
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="slide-menu">
        <% if user_signed_in? %>
          <div class="slide-menu-user-name"><%= current_user.name %></div>
          <ul class="slide-menu-list">
            <li>
              <span>
                <% if @standbyRules.present? || @standbyPenalties.present? || @unacceptedUsers.present? %>
                  <i class="fa-solid fa-circle"></i>
                <% end %>
              </span>
              <span><%= link_to "通知BOX", receive_path %></span>
            </li>
            <li><%= link_to "コミュニティ一覧", top_path %></li>
            <li><%= link_to "ユーザー情報", edit_user_path(current_user.id) %></li>
            <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
            <li>利用規約</li>
            <li>プライバシーポリシー</li>
            <li><%= link_to "TOP", home_path %></li>
          </ul>
        <% else %>
          <ul class="slide-menu-list">
            <li><%= link_to "TOPページ", root_path %></li>
            <li><%= link_to "新規登録", new_user_registration_path %></li>
            <li><%= link_to "ログイン", new_user_session_path %></li>
            <li>利用規約</li>
            <li>プライバシーポリシー</li>
          </ul>
        <% end %>
      </div>
    </header>
    <main>
      <%= yield %>
    </main>
    <script>
      //コメントの表示/非表示
      $(document).ready(function() {
        $(".fa-circle-info").click(function() {
          $(this).siblings(".comment-content").toggle();
        });
      });
    </script>
    <script>
      //サイドメニュースライド
      document.querySelector('.sign-in-user-profile-image').addEventListener('click', function(){
        this.classList.toggle('active');
        document.querySelector('.slide-menu').classList.toggle('active');
      })
    </script>
  </body>
</html>
