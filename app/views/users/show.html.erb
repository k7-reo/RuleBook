  <h2><%= @user.user.name %>の詳細</h2>

  <div>
    <div>
      <p>今月のポイント獲得進捗：<%= @user.point %></p>
    </div>
    <div>
      <p>今月利用できるポイント：<%= @user.monthly_point %></p>
    </div>
    <!-- 先月違反した&継続したルール一覧withポイント -->
    <!-- 今月違反した&継続したルール一覧withポイント -->
    <div>
      <h3><%= @user.user.name %>が作成したルール</h3>
      <div>
        <% if @rules.present? %>
          <table>
            <thead>
              <tr>
                <th>内容</th>
                <th>増減ポイント</th>
              </tr>
            </thead>
            <tbody>
            <% @rules.each do |rule| %>
              <tr>
                <td><%= rule.content %></td>
                <td><%= rule.point %></td>
              </tr>
            <% end %>
            </tbody>
          </table>
        <% else %>
          <p>あなたが<%= @community.community_name %>で作成したルールはありません。</p>
        <% end %>
      </div>
    </div>
    <div>
      <h3><%= @user.user.name %>が作成した特典</h3>
      <div>
        <% if @privileges.present? %>
          <table>
            <thead>
              <tr>
                <th>内容</th>
                <th>利用ポイント</th>
              </tr>
            </thead>
            <tbody>
            <% @privileges.each do |privilege| %>
              <tr>
                <td><%= privilege.content %></td>
                <td><%= privilege.point %></td>
              </tr>
            <% end %>
            </tbody>
          </table>
        <% else %>
          <p>あなたが<%= @community.community_name %>で作成した特典はありません。</p>
        <% end %>
      </div>
    </div>
    <div>
      <h2><%= @user.user.name %>が作成した罰</h2>
      <div>
        <% if @penalties.present? %>
          <table>
            <thead>
              <tr>
                <th>内容</th>
                <th>回復ポイント</th>
              </tr>
            </thead>
            <tbody>
            <% @penalties.each do |penaltie| %>
              <tr>
                <td><%= penaltie.content %></td>
                <td><%= penaltie.point %></td>
              </tr>
            <% end %>
            </tbody>
          </table>
        <% else %>
          <p>あなたが<%= @community.community_name %>で作成したペナルティはありません。</p>
        <% end %>
      </div>
    </div>
  </div>

  <div>
    <%= link_to "コミュニティTopへ", community_path(@community.id) %>
  </div>