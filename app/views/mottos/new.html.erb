  <h1>モットー作成</h1>

  <div>
  <%= form_with(model: [@community, @motto], local:true) do |f| %> <!-- communityモデル、mottoモデルそれぞれにデータを保存するため、両モデル指定-->
   <p>モットー</p>
   <%= f.text_area :content, placeholder: "モットーをここに" %>
   <%= f.submit '作成' %>
  <% end %>
  </div>
  <div>
   <P><%= link_to "コミュニティTopへ", community_path(@community.id) %></P>
  <div>