  <div class="community-background">
    <%= attachment_image_tag @community, :community_image, format: 'jpeg' %>
  </div>
  <div class="community-info">
    <%= render 'communities/info', community: @community, currentUser: @currentUser %>
  </div>
  <div class="community-main">
    <p class="page-title">スローガン編集</p>
    <div class="comment">
      <p>制定ポイント</p>
      <p>
        ゴール達成のためにメンバー全員が心がけることを記載してください。<br>
        みんなで話し合って決めてみましょう！
      </p>
    </div>
    <div>
      <%= form_with(model:[@community, @motto], local:true) do |f| %>
        <div>
          <label><%= f.label :content, "内容" %><span class="form-note">必須</span></label>
          <div>
          <%= f.text_area :content %>
          </div>
        </div>
        <div>
        <%= f.submit "編集完了" %>
        </div>
      <% end %>
    </div>
    <div>
      <p><%= link_to "削除", community_motto_path(@community.id, @motto.id), method: :delete %></p>
    </div>
  </div>