  <div class="community-info">
    <%= render 'communities/info', community: @community, currentUser: @currentUser %>
  </div>

  <div class="community-main">
    <p class="page-title">報酬編集</p>
    <div class="comment">
      <p>
        <b>制定ポイント</b><br>
        コミュニティのゴール達成に向けて<br>
        更に意欲が湧くような報酬はなんですか？
      </p>
    </div>
    <div>
      <%= form_with(model:[@community, @privilege], local:true) do |f| %>
        <label>報酬</label>
        <div>
        <%= f.text_area :content %>
        </div>
        <label>受けることによって消費する配当ポイント</label>
        <div>
        <%= f.text_area :point %>
        </div>
        <div>
        <%= f.submit "編集完了" %>
        </div>
      <% end %>
    </div>
  </div>